import pandas as pd
import matplotlib.pyplot as plt
from typing import Optional


def plot_forecast(
    ts: pd.Series,
    ts_pred: pd.Series,
    forecast_origin: pd.Timestamp,
    metric: float,
    metric_name: str,
    model: str,
    save: Optional,
):
    pass
